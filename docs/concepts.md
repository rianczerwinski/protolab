# Concepts

Standalone essay on error-driven compression for protocol documents.

## The Accumulation Problem

[TODO] Why protocol improvement defaults to growth. Every correction adds a rule; no process removes them. The document becomes its own archaeology.

## The Compression Insight

[TODO] Structured error accumulation + periodic resynthesis = convergence. The protocol gets denser, not longer. Information density increases per cycle.

## The Refinement Cycle

[TODO] Formal description: Protocol(v) + Corrections(v) -> Resynthesis -> Protocol(v+1). Each cycle is a compression event. The cycle is the unit of improvement, not the individual correction.

## Error as Feedstock

[TODO] Corrections aren't problems to fix. They're the energy source for refinement. Without errors, the protocol stagnates. The tool reframes failure as fuel.

## Rules as Crystallized Pattern

[TODO] The intermediate representation between raw corrections and integrated protocol. Rules are where pattern recognition becomes explicit. Corrections say "this was wrong"; rules say "here's the generalizable discriminator."

## Convergence and the Eigenform

[TODO] The protocol approaches a fixed point: the version that no longer produces errors driving further modification. Never reached; asymptotically approached. Brief nod to von Foerster, accessible register.

## The Elaboratorium

[TODO] Etymology of protolab. The archaic sense of "elaborate" as purification through careful process. The lab as ongoing workspace, never finished.

## Domain Generality

[TODO] Why this works for any decision-guiding document. Prompt engineering as entry point; the general pattern as depth.
